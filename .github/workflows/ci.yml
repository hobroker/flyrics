name: CI/CD

on: push

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: git clone https://github.com/flutter/flutter.git -b dev --depth 1
      - run: echo "::set-env name=PATH::$PATH:`pwd`/flutter/bin"
#      - run: flutter precache
      - run: flutter build macos
